apiVersion: apps/v1
kind: Deployment
metadata:
  name: pedidos-dep
  namespace: fast-order
  labels:
    app: pedidos-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pedidos-api
  template:
    metadata:
      labels:
        app: pedidos-api
    spec:
      containers:
      - name: pedidos-api-cont
        image: sickbanger/pedidos-api:latest
        ports:
        - containerPort: 8080
        - containerPort: 8081
        envFrom:
          - configMapRef:
              name: pedidos-api-configmap
          - secretRef:
              name: pedidos-api-secrets
        env:
          - name: DB_CONNECTION_STRING
            valueFrom:
              configMapKeyRef:
                name: pedidos-api-configmap
                key: DB_CONNECTION_STRING
        livenessProbe:
          httpGet:
            path: /swagger
            port: 8080
          initialDelaySeconds: 20
          periodSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /swagger
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
          failureThreshold: 3
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
